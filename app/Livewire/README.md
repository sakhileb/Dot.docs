# Livewire Components

This directory is organized by domain to keep feature growth manageable:

- `Documents` for listing, CRUD workflows, and sharing
- `Editor` for rich text editing and collaboration widgets
- `AI` for assistant, prompts, and generation workflows
